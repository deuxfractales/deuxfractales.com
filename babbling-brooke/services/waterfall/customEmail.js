exports.customEmail = (buyerName, buyerEmail, fileNames, contract) => {
    fileNames.forEach(file)
  if (contract === 'mp3') {
    const msg = {
      to: buyerEmail,
      from: 'marketing@circlebeats.com',
      subject: 'CIRCLEBEATS.COM',
      text: `Thanks for your purchase ${buyerName}`,
      html: '<strong>and easy to do anywhere, even with Node.js</strong>',
    };
  }
};
